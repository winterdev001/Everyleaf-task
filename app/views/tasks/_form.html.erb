<%= form_with(model: @task, local: true) do |form| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% @task.errors.full_messages.each do |message| %>
          <li class="text-danger"><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :task_name , placeholder:"Task name", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.text_field :label_name ,placeholder:"Label name", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.date_field :start_date, as: :date, value: form.object.try(:strftime,"%d/%m/%Y"), class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.date_field :end_date, as: :date, value: form.object.try(:strftime,"%d/%m/%Y"), class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.text_field :status ,placeholder:"Status", class: "form-control" %>
  </div>
  <hr>  
  <div class="actions text-center">
    <%= form.submit  class:"btn btn-dark btn-block rounded-pill"  %>    
  </div>
<% end %>
   